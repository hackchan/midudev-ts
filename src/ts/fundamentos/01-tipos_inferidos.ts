/* el Motor de inferencia asigna el tipo de dato
automaticamente */
(() => {
  let myProductName = 'Product 1'
  let myProductPrice

  myProductName = 'car'
  myProductName.toLocaleLowerCase()

  myProductPrice = 1000
  myProductPrice = myProductPrice = 100
  myProductPrice.toFixed()

  console.log(`name: ${myProductName}`)
  console.log(`price: $${myProductPrice}`)

  const stock = 1000
  console.log('stock in:', stock)
})()
