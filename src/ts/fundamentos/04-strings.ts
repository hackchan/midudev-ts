(() => {
  const myProductPrice = 100
  let productTitle = 'my product'
  productTitle = 'My amazin product'
  console.log(productTitle)
  const summary: string = `
  title: ${productTitle}
  price: ${myProductPrice}`

  console.log(summary)
})()
